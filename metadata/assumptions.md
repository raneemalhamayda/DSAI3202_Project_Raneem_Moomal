
# Assumptions

- Top-level genres are used for Phase 1
- Batch ingestion is used
- Raw data remains unchanged
- Missing or invalid records are removed during ETL
- Feature extraction may be run on a subset if compute or time is limited
