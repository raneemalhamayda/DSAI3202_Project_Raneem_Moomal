
# Data Lineage

Raw audio and metadata are stored in the raw layer.
Metadata is cleaned in the ETL process.
Cleaned metadata is matched with audio file paths.
A final manifest is generated for training and evaluation.
Features are extracted from the processed dataset.
